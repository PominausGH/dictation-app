faster-whisper>=1.0.0
numpy
pyaudio>=0.2.14
webrtcvad>=2.0.10
pystray>=0.19.0
Pillow>=10.0.0
anthropic>=0.40.0
platformdirs>=4.0.0
evdev>=1.6.0; sys_platform == "linux"
pynput>=1.7.6; sys_platform == "win32"

# openwakeword is intentionally not listed above: its tflite-runtime dependency
# has no wheel coverage on current Python (Linux or Windows). It falls back to
# onnxruntime at import time when tflite_runtime is absent, so setup.sh/setup.ps1
# install it separately with --no-deps plus its real deps. See packaging/aur/PKGBUILD
# for the same workaround. The app degrades gracefully (Alt+D only) if it's missing.
